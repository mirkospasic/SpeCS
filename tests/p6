superquery:
PREFIX : <http://www.example.org/>

SELECT ?x ?y WHERE {
	?x a :Professor .
	?x :memeberOf ?y 
}


subquery:
PREFIX : <http://www.example.org/>

SELECT ?x ?y WHERE {
	?x a :Professor .
	?x :graduatedFrom ?y .
	?x :memeberOf ?y 
}
